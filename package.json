{
  "name": "tokenpulse",
  "version": "1.0.0",
  "module": "src/index.ts",
  "type": "module",
  "scripts": {
    "start": "bun run src/lib/migrate.ts && bun run src/index.ts",
    "dev": "bun --watch src/index.ts",
    "test": "bun test",
    "test:coverage": "bun test --coverage",
    "db:push": "bun drizzle-kit push",
    "db:studio": "bun drizzle-kit studio"
  },
  "dependencies": {
    "@hono/zod-validator": "^0.7.6",
    "drizzle-kit": "latest",
    "drizzle-orm": "latest",
    "hono": "^4.0.0",
    "sharp": "^0.34.5",
    "zod": "^3.0.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "better-sqlite3": "^12.6.0"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
